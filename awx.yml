---
- name: test
  hosts: all
  gather_facts: no
  connection: local

  tasks:
    - name: Read file in hostname
      shell: bash /var/lib/awx/log/FA_EX3400_VC_A/messages_2022-02-24.log
      register: hostname
      ignore_errors: True
